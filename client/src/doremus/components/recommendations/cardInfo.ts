export class RecommendationCardInfo {
  id: string;
  title: string;
  subtitle:string;

  constructor(id, title, subtitle)
  {
    this.id = id;
    this.title = title;
    this.subtitle = subtitle;
  }
}
