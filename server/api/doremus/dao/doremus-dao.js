//NOT USED
import mongoose from 'mongoose';
import Promise from 'bluebird';
import doremusSchema from '../model/doremus-model';
import _ from 'lodash';

doremusSchema.statics.getAll = () => {
  return new Promise((resolve, reject) => {
    var _query = {};
  });
}
